class Sphere {
    constructor(scene){
        this.x = x;
        this.y = y;
        this.z = z;
        this.radius = 0.2;
        this.color = white;

        this.draw = function() {
            let light  = scene.createElement('a-sphere');
            light.setAttribute('radius', this.radius);
            light.setAttribute('')
            
        }
    }
}